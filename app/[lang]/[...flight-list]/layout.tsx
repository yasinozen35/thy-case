import { LayoutPropsType } from "@/utils/types/commonTypes";

const FlightListLayout = ({ children }: Readonly<LayoutPropsType>) => {
  return <div>{children}</div>;
};
export default FlightListLayout;
